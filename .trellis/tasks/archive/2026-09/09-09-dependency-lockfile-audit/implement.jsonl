{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "R1/R4 invariants: camoufox==0.5.4 is a browser-build contract (do not bump — re-run pass-rate matrix); pytest must stay green; verification command is `source .venv/bin/activate && pytest`. Lockfile must be additive (no pyproject dependency change)."}
{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Repo is a flat single package (src/argus/, tests/, docs/) with no scripts/ dir yet — decides where requirements.lock (repo root) and the pip-audit entrypoint (scripts/audit.sh or a make audit target) belong; env-var convention if audit needs config."}
